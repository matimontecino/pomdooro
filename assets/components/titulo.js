import { View,Text } from "react-native";
export default const Titulo = ({Titulo}) => {
    return(
        <View>
                <Text>
                  {Titulo}
                </Text>
            </View>
    )
};

export default Titulo;